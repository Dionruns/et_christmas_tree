# 🚀 Vercel 部署指南（推荐国内使用）

## 为什么选择 Vercel？

- ✅ **完全免费**
- ✅ **国内访问速度好**（比 jsDelivr 稳定）
- ✅ **自动 HTTPS**
- ✅ **自动 CDN 加速**
- ✅ **部署超简单**（一键部署）
- ✅ **支持自定义域名**

---

## 📦 方案 A：直接部署整个项目（最简单）

### 1️⃣ 准备项目

不需要单独的 CDN，Vercel 会自动处理所有静态资源。

修改 `src/config.ts`：

```typescript
// 使用相对路径，Vercel 会自动 CDN 加速
export const CDN_BASE_URL = '';

// 或者使用环境变量
export const CDN_BASE_URL = import.meta.env.VITE_CDN_BASE_URL || '';
```

### 2️⃣ 部署到 Vercel

**方法 1：使用 Vercel CLI（推荐）**

```bash
# 安装 Vercel CLI
npm install -g vercel

# 登录
vercel login

# 部署
cd christmas-tree
vercel
```

**方法 2：使用 GitHub 集成**

1. 将项目推送到 GitHub
2. 访问 https://vercel.com
3. 点击 "Import Project"
4. 选择你的 GitHub 仓库
5. 点击 "Deploy"

### 3️⃣ 完成

部署完成后，Vercel 会给你一个域名：
```
https://your-project.vercel.app
```

所有资源都会自动通过 Vercel CDN 加速！

---

## 📦 方案 B：分离部署（CDN + 主站）

如果你想要更灵活的控制：

### 1️⃣ 创建 CDN 项目

创建一个新的 Vercel 项目专门存放静态资源：

```bash
# 创建新文件夹
mkdir christmas-tree-cdn-vercel
cd christmas-tree-cdn-vercel

# 复制 cdn-assets 内容
cp -r ../christmas-tree/cdn-assets/* .

# 创建 vercel.json
cat > vercel.json << EOF
{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        },
        {
          "key": "Access-Control-Allow-Origin",
          "value": "*"
        }
      ]
    }
  ]
}
EOF

# 部署
vercel
```

### 2️⃣ 配置主项目

修改 `src/config.ts`：

```typescript
export const CDN_BASE_URL = import.meta.env.PROD 
  ? 'https://your-cdn-project.vercel.app'
  : '';
```

### 3️⃣ 部署主项目

```bash
cd christmas-tree
vercel
```

---

## 🌐 方案 C：使用国内 CDN（最快）

如果需要最快的国内访问速度：

### 阿里云 OSS + CDN

**费用：** 约 ¥0.12/GB（流量费）+ ¥0.02/GB（存储费）

**步骤：**
1. 注册阿里云账号
2. 开通 OSS 服务
3. 创建 Bucket
4. 上传 `cdn-assets` 文件夹
5. 开启 CDN 加速
6. 获取 CDN 域名

**配置：**
```typescript
export const CDN_BASE_URL = import.meta.env.PROD 
  ? 'https://your-bucket.oss-cn-hangzhou.aliyuncs.com'
  : '';
```

### 腾讯云 COS + CDN

**费用：** 类似阿里云

**步骤：**
1. 注册腾讯云账号
2. 开通 COS 服务
3. 创建存储桶
4. 上传文件
5. 开启 CDN
6. 获取 CDN 域名

**配置：**
```typescript
export const CDN_BASE_URL = import.meta.env.PROD 
  ? 'https://your-bucket.cos.ap-guangzhou.myqcloud.com'
  : '';
```

---

## 📊 方案对比

| 方案 | 费用 | 国内速度 | 配置难度 | 推荐度 |
|------|------|---------|---------|--------|
| Vercel 整站部署 | 免费 | ⭐⭐⭐⭐ | ⭐ | ⭐⭐⭐⭐⭐ |
| Vercel 分离部署 | 免费 | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐ |
| jsDelivr | 免费 | ⭐⭐ | ⭐ | ⭐⭐ |
| 阿里云 OSS | 付费 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 腾讯云 COS | 付费 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| Cloudflare R2 | 免费额度 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |

---

## 💡 我的推荐

### 个人项目/演示：
👉 **Vercel 整站部署**（方案 A）
- 最简单
- 完全免费
- 国内速度好

### 商业项目：
👉 **阿里云 OSS + CDN**
- 速度最快
- 最稳定
- 费用可控（约 ¥10-50/月）

---

## 🚀 快速开始（Vercel）

```bash
# 1. 安装 Vercel CLI
npm install -g vercel

# 2. 登录
vercel login

# 3. 部署
cd christmas-tree
vercel

# 4. 完成！
# 访问 Vercel 给你的域名
```

---

## 📝 注意事项

1. **Vercel 免费版限制：**
   - 每月 100GB 带宽
   - 对于个人项目完全够用

2. **自定义域名：**
   - Vercel 支持免费绑定自定义域名
   - 需要在域名商处添加 CNAME 记录

3. **环境变量：**
   - 可以在 Vercel 控制台设置环境变量
   - 用于区分开发和生产环境

---

## ❓ 常见问题

**Q: Vercel 在国内访问快吗？**
A: 比 jsDelivr 快且稳定，但不如国内 CDN

**Q: 需要备案吗？**
A: 使用 Vercel 域名不需要备案，自定义域名需要

**Q: 如何更新文件？**
A: 重新部署即可，Vercel 会自动更新

**Q: 费用如何？**
A: 个人项目完全免费，商业项目可能需要升级

---

## 🎯 总结

- **最简单**：Vercel 整站部署
- **最快**：阿里云/腾讯云 CDN
- **最便宜**：Vercel（免费）
- **最灵活**：Cloudflare R2

根据你的需求选择合适的方案！🎄✨
